fn mktok(t, v) {
    return {type: t, value: v};
}

tok <- mktok("NUMBER", "42");

if (tok.type = "NUMBER") {
    print("Got number: " + tok.value);
}

if (tok.type = "STRING") {
    print("Got string");
}

print("done");