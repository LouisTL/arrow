// EXPECT: type_fail
//ERR: union inside an array or struct is not supported yet

// 3a lifted the restriction for array-of-union *literals* at a variable
// declaration (see array_union_test). Passing or returning a union-element
// array is still unsupported (deferred), so a [int | str] parameter is rejected.
fn first(xs: [int | str]): int {
    return 0;
}
print(first([1]));
