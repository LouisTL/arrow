// EXPECT: type_fail
//ERR: is not a member of

// An arm naming a struct shape that isn't in the union is rejected.
var s: {a: int} | {b: int} <- {a: 1};
match (s) {
    {a: int} => { print(s.a); }
    {b: int} => { print(s.b); }
    {c: int} => { print(0); }
}
